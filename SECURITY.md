# 安全政策（Security Policy）

## 支持的版本

本项目当前仅对主分支（main）提供安全更新支持。

| 版本 | 支持状态 |
|------|---------|
| main | ✅ 支持 |
| 其他分支 / 旧版本 | ❌ 不支持 |

## 报告漏洞

我们非常重视本项目的安全问题。如您发现安全漏洞，请按以下方式报告：

### 报告方式

**推荐方式**：使用 GitHub Security Advisory

1. 访问 https://github.com/mdoe123/Not-too-open-eewapp/security/advisories/new
2. 点击 "Report a vulnerability"
3. 填写漏洞描述、复现步骤、影响范围

**备用方式**：通过 GitHub Issues（请勿在公开 Issue 中包含敏感信息）

> ⚠️ 请勿通过公开 Issue 提交可能被利用的漏洞详情。请先使用 Security Advisory 私密报告，待修复后再公开讨论。

### 报告内容要求

为帮助我们快速评估和处理漏洞，请在报告中包含：

1. **漏洞描述**：清晰说明漏洞类型和性质
2. **影响范围**：漏洞可能造成的影响（如数据泄露、权限提升、服务中断等）
3. **复现步骤**：详细的复现步骤，包括环境信息（Android 版本、设备型号、App 版本）
4. **证明材料**：如可能，提供截图、日志或 PoC（Proof of Concept）
5. **建议修复方案**：如您有修复建议，请一并告知

### 响应时间

| 阶段 | 时间 |
|------|------|
| 确认收到报告 | 72 小时内 |
| 初步评估 | 7 天内 |
| 修复方案制定 | 30 天内（视漏洞严重程度） |
| 修复发布 | 90 天内（视漏洞复杂度） |

## 披露政策

- 我们遵循**负责任披露**（Responsible Disclosure）原则
- 漏洞修复并发布新版本后，我们将公开披露漏洞详情
- 在修复前，我们不会公开漏洞详情，以免被恶意利用
- 报告者如有意愿，将在漏洞披露时被致谢（除非要求匿名）

## 安全最佳实践（面向用户）

使用本应用时，建议遵循以下安全实践：

### 数据源配置

- ✅ **使用 HTTPS/WSS 端点**：配置 customSource 时优先使用 `https://` 或 `wss://` 协议，避免明文传输
- ✅ **可信数据源**：仅连接您信任的数据源端点，避免连接来源不明的服务
- ⚠️ **authToken 安全**：authToken 存储在本地，不会出现在日志中，但请勿在公开场合分享包含 authToken 的数据源配置

### 权限管理

- ✅ **最小权限原则**：仅授予应用必需的权限。如不使用闪光灯警报，可在系统设置中撤销相机权限
- ✅ **定期审查**：定期检查应用权限和数据源配置，移除不再使用的源

### 应用来源

- ✅ **从可信来源获取**：仅从 [GitHub Releases](https://github.com/mdoe123/Not-too-open-eewapp/releases) 或可信分发渠道获取本应用
- ⚠️ **验证签名**：如可能，验证 APK 签名是否与官方发布一致

## 已知安全考虑

### 本地存储

应用配置（包括数据源 URL、authToken）存储在本地 AsyncStorage 和 SharedPreferences 中。在已 root 的设备上，这些数据可能被其他应用读取。建议：

- 不要在 root 设备上使用本应用配置包含敏感 authToken 的数据源
- 如需在 root 设备上使用，请使用不含敏感信息的数据源

### 网络通信

应用直连用户配置的数据源端点，网络通信内容不经过任何中间服务器。但如使用 `http://` 或 `ws://` 明文协议，通信内容可能被网络中间人截获。建议使用 `https://` 或 `wss://`。

## 联系方式

如有任何安全问题，请通过以下方式联系：

- **GitHub Security Advisory**：https://github.com/mdoe123/Not-too-open-eewapp/security/advisories/new
- **GitHub Issues**：https://github.com/mdoe123/Not-too-open-eewapp/issues（非敏感问题）

---

感谢您帮助本项目保持安全。
